#include <stdio.h>

void Main() {
    float a = 10;
    float b = 5;
    
    if (b = 5) {  // we wrote = instead of ==
        printf("hii");
    } else {
        printf("The output is: %f", a/b);
    }
}
